/*
实现步骤
    必将当前元素和下一个元素，移动下一个元素

*/
const arr = [6, 1, 2, 7, 9, 3, 4, 5, 10, 8];


const bubbleSort = (array) => {

    for (let i = 0; i < array.length; i+=1) {

        for (let j = i + 1; j < array.length; j+=1) {

        
        }
    }
}